import 'package:flutter/material.dart';

class SelectSubjectsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(child: Text('Select the Subjects Page')),
    );
  }
}
